console.log( "ready") ;

playagain  = 
var prize1 = "car";
var prize2 = "cat";
var proze3 = "baloon";

var doors = [ prize1, prize2, prize3 ];

function askuser(){
	

	Var userResponse = prompt("enter a number between 1 and 3...");

function processResponse()};
}


//test that the userResponse
//get the datatyoe after parseint()

console.log( userResponse);
//Remove 1 to the userResponse before alert
userResponse = userResponse -1;

//userResponse is either 1,2,3
if(userResponse ===1 || userResponse ===2 || userResponse ===3||) {
	//were good
	//remove 1 to the username before alert
	userResponse = userResponse - 1;
	//userRespoonse is either 1,2,3
	alert( doors[userResponse] )
}else{
	//ask again
	after( "sorry we have no doors name like that")
	}
}

//start game - intake
askuser();
